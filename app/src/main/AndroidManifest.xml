<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.quidditchcounter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Quidditch Scorekeeper" />
        <activity
            android:name=".startPage"
            android:label="Start Page- Who&apos;s Playing?">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".firstOvertime"
            android:label="Quidditch Scorekeeper- First Overtime" />
        <activity
            android:name=".secondOvertime"
            android:label="Quidditch Scorekeeper- Second Overtime" />
        <activity
            android:name=".matchSummary"
            android:label="Match Summary" />
    </application>

</manifest>